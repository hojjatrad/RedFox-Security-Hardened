# تعمیر Runtime خرید ربات نماینده — 2.3.6

علت اصلی عدم واکنش دکمه خرید، مقدار پیش‌فرض اشتباه `show_product=true` در ربات‌هایی بود که از پنل وب ساخته می‌شدند؛ در منطق قدیمی true به مسیر حجم دلخواه می‌رفت و اگر حجم دلخواه خاموش بود، محصولی نمایش نمی‌داد. علاوه بر آن مسیر خرید به mysqli وابسته بود و روی هاست‌هایی که PDO سالم ولی mysqli ناموفق بود، Exception به‌صورت خاموش پایان می‌یافت.

اصلاحات:
- مهاجرت Runtime یک‌باره `product_mode_version=2` و `show_product=false`.
- تعمیر همه تنظیمات botsaz در Installer و دکمه تعمیر مدیریت نمایندگان.
- جایگزینی mysqli شمارش پنل با PDO fetchAll.
- جایگزینی rowCount روی SELECT محصول با fetchAll count.
- Handler استثنا اکنون کد پیگیری را به کاربر می‌دهد و دیگر «هیچ اتفاقی» رخ نمی‌دهد.
- Debug log ورود قطعی به BUY_HANDLER را ثبت می‌کند.
- متن سفارشی/قدیمی دکمه و callbackهای buy/buyback پوشش داده می‌شوند.
