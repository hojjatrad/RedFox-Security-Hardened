# 📊 ممیزی پنل تحت وب Red Fox — چه چیزهایی قابل‌مدیریت است

> تاریخ: ۱۴۰۵/۰۵/۱۸ (۲۰۲۶-۰۸-۰۹)
> این سند نقشه‌ی کاملی از بخش‌های ربات و وضعیت مدیریت آن‌ها در پنل تحت وب است.

---

## ۱) صفحه‌هایی که الان در پنل وب موجوده (۲۸ صفحه)

| دسته | صفحه‌ها |
|------|---------|
| **کاربران/مالی** | users, user, invoice, payment, finance, cancelService, **agents** ✅ (جدید) |
| **پنل‌ها/محصولات** | panels, product, productedit, inbound, stock, seeting_x_ui |
| **فروشگاه** | shopsettings, discounts, keyboard, service_keyboard |
| **ظاهر/متن** | appearance, text, textbot, applinks |
| **سیستم** | index, settings, broadcast, login, service |

---

## ۲) نقشه‌ی سیستم‌های ربات + وضعیت مدیریت در پنل وب

| سیستم ربات | جدول/منبع دیتا | در پنل وب؟ | وضعیت |
|-----------|---------------|-----------|-------|
| محصولات | `product` (category, agent, Location…) | ✅ هست (`product.php`) | قابل ارتقا (مدیریت دسته‌بندی) |
| **دسته‌بندی محصولات** | `category` (id, remark) + `product.category` | ❌ **نیست** | فقط فیلد متنی؛ صفحه‌ی مدیریت دسته‌ها لازم است |
| **آموزش/راهنما** | `help` (name_os, Media_os, type_Media_os, category, Description_os) | ❌ **نیست** | فقط از داخل ربات ادمین |
| **نرم‌افزارهای اتصال** | `app` (id, name, link) | ❌ **نیست** | فقط از داخل ربات |
| **ربات نماینده (botsaz)** | `botsaz` (id_user, bot_token, admin_ids, username, setting) + پوشه‌ی `vpnbot/{id}{user}` | ❌ **نیست** | فقط از داخل ربات (پیچیده‌ترین) |
| برند/نام ربات | `textbot`, `setting`, تصاویر | ✅ بخشی (`appearance`, `text`, `textbot`) | — |
| پنل‌ها/اینباند | `marzban_panel` | ✅ هست (`panels.php`) | — |
| کدهای تخفیف/هدیه | `Discount`, `Giftcode` | ✅ هست (`discounts.php`) | — |
| نماینده‌ها | `user.agent`, `Requestagent` | ✅ هست (`agents.php`) ✅ (جدید) | — |
| پیام همگانی | — | ✅ هست (`broadcast.php`) | — |

---

## ۳) سه قابلیت درخواستی — وضعیت، پیچیدگی، ریسک

### 🔵 الف) مدیریت ربات نماینده (فعال‌سازی + برند + محصولات نماینده)
- **منبع:** جدول `botsaz` + کپی فیزیکی پوشه‌ی `vpnbot/Default` به `vpnbot/{id}{username}` + تنظیم webhook با توکن نماینده.
- **الان:** فقط از داخل ربات ادمین (`maintenance.php`).
- **پیچیدگی: 🔴 بالا** — چون شامل: ساخت/حذف ربات اختصاصی برای هر نماینده، کپی فایل، set/delete webhook تلگرام، ویرایش `product.json` و `text.json` (برند) هر ربات.
- **ریسک:** بالا (عملیات فایل + API تلگرام + چندین نمونه‌ی مجزا). حتماً باید روی سرور واقعی تست شود.

### 🟢 ب) مدیریت دسته‌بندی محصولات در پنل وب
- **منبع:** جدول `category` + فیلد `product.category`.
- **الان:** فقط فیلد متنی آزاد در فرم محصول.
- **پیچیدگی: 🟢 پایین** — یک صفحه‌ی CRUD ساده (افزودن/ویرایش/حذف دسته) + تبدیل فیلد دسته‌ی فرم محصول به dropdown.
- **ریسک:** پایین (داده‌های کم‌حساسیت).

### 🟡 ج) مدیریت آموزش‌ها + نرم‌افزارهای اتصال در پنل وب
- **منبع:** جدول `help` (آموزش: متن/عکس/ویدیو + دسته) و جدول `app` (نرم‌افزار: نام + لینک).
- **الان:** فقط از داخل ربات.
- **پیچیدگی: 🟡 متوسط** — دو صفحه: مدیریت آموزش (با آپلود رسانه) + مدیریت نرم‌افزارها (نام/لینک).
- **ریسک:** متوسط (آپلود فایل رسانه نیاز به دقت دارد).

---

## ۴) پیشنهاد ترتیب اجرا (از کم‌ریسک به پر‌ریسک)

1. **ب) مدیریت دسته‌بندی محصولات** — پایین‌ترین ریسک، سریع‌ترین، ارزش بالا.
2. **ج) مدیریت آموزش‌ها + نرم‌افزارها** — ریسک متوسط، مستقل.
3. **الف) مدیریت ربات نماینده** — بالاترین ریسک؛ باید با احتیاط و احتمالاً به‌صورت مرحله‌ای (اول فقط لیست/فعال‌سازی، بعد برند و محصولات).

---

## ۵) فهرست کامل چیزهایی که می‌توان برای پنل وب ساخت

علاوه بر ۳ مورد بالا، این بخش‌ها هم فعلاً فقط از داخل ربات مدیریت می‌شوند و می‌توان به پنل وب آورد:

- تنظیمات قرعه‌کشی/چرخ‌وشرکت (Lottery/wheel)
- مدیریت دپارتمان‌های پشتیبانی (`departman`)
- مدیریت شماره کارت‌های کارت‌به‌کارت (`card_number`)
- مدیریت آیدی پشتیبانی و کانال‌ها
- گزارش‌های آماری پیشرفته (فروش/کاربر فعال)
- برون‌بری (Excel/CSV) گزارش‌ها
- سیستم کش‌بک جداگانه برای هر درگاه

---

🦊 *Red Fox — GPLv3*
