# رفع نهایی ظرفیت ربات نماینده و ذخیره یکپارچه نماینده — 2.3.3

## علت واقعی تکرار خطا
قالب‌های `vpnbot/Default` و `vpnbot/update` هنوز منطق قدیمی را داشتند: در حالت تک‌پنل تمام فاکتورهای همه پنل‌ها با `rowCount()` شمرده می‌شدند. بنابراین همگام‌سازی نیز همان فایل معیوب را روی ربات‌های ساخته‌شده کپی می‌کرد.

## اصلاح ظرفیت
- محاسبه داخل خود قالب ربات نماینده قرار گرفت و به Helper خارجی وابسته نیست.
- فقط فاکتورهای همان `Service_location` شمرده می‌شوند.
- فقط وضعیت‌های فعال، در انتظار و هشدار فعال شمرده می‌شوند؛ سرویس منقضی ظرفیت فروش را دائماً اشغال نمی‌کند.
- از `COUNT(*)` استفاده می‌شود، نه `rowCount()` روی SELECT.
- empty، null، zero، -1، unlimited، unlimted و نامحدود همگی نامحدود محسوب می‌شوند.
- جزئیات used/limit/panel در error log نوشته می‌شود.
- Migration 023 مقادیر قدیمی ظرفیت را نرمال می‌کند.

## مدیریت نماینده
- فرم یکپارچه نوع، نقش، والد، وضعیت پورتال و سقف بدهی اضافه شد.
- دکمه `💾 ذخیره تغییرات نماینده` همه موارد را Transactional ذخیره می‌کند.
- بعد از ذخیره، فایل‌های اجرایی ربات همان نماینده خودکار از قالب اصلاح‌شده همگام می‌شوند.
- دکمه POST امن برای همگام‌سازی همه ربات‌های نماینده جایگزین لینک GET قبلی شد.
- config.php، token، style، text.json و data ربات‌های نماینده حفظ می‌شوند.
