# راهنمای فاز دوم — پنل نماینده و سوپرنماینده

## پیش‌نیاز

ابتدا فاز امنیتی و migration شماره 001 نصب شده باشد. سپس:

```sql
SOURCE migrations/002_reseller_portal.sql;
```

یا فایل `002_reseller_portal.sql` را از phpMyAdmin اجرا کنید. این migration داده موجود را حذف نمی‌کند.

## مدل نقش

- `agent`: فقط فروش، کاربران و ربات خودش.
- `super`: داده خودش به‌اضافه زیرنمایندگان **مستقیم** که ادمین با `reseller_parent_id` به او متصل کرده است.
- رابطه افیلیت برای ساخت سلسله‌مراتب استفاده نمی‌شود؛ بنابراین مشتری دعوت‌شده با زیرنماینده اشتباه نمی‌شود.

## تعیین نقش از پنل ادمین

پنل ادمین ← مدیریت نمایندگان ← کارت نماینده:

- نقش: نماینده یا سوپرنماینده
- آیدی والد: فقط آیدی یک سوپرنماینده سطح بالا؛ برای مستقل‌بودن خالی بماند
- وضعیت پورتال: فعال/غیرفعال

مهاجرت، نماینده‌های قدیمی دارای `reseller_perms` را برای حفظ رفتار قبلی به نقش `super` تبدیل می‌کند. پس از نصب، نقش‌ها را بازبینی کنید.

## ماژول‌های پنل جدید

- داشبورد مشابه پنل اصلی
- مشتریان و پروفایل مشتری
- سفارش‌ها و CSV
- جزئیات سرویس و کانفیگ متعلق به همان محدوده
- پرداخت‌های متصل به فاکتور یا ربات اختصاصی
- گزارش فروش و تفکیک زیرنماینده
- قیمت و نام اختصاصی محصولات، با کف قیمت پایه
- فعال/غیرفعال‌کردن دسته‌بندی در ربات
- کارت بانکی اختصاصی
- برند، متن‌ها و رنگ دکمه‌ها
- تنظیم قیمت حجم/زمان و تنظیمات whitelist شده ربات
- مدیریت زیرنمایندگان مستقیم
- انتقال اعتبار تراکنشی با ledger
- لاگ فعالیت
- تغییر رمز حساب

## کنترل مالکیت داده

Scope فقط در UI نیست. تمام Queryهای حساس از یکی از این دو شاخص استفاده می‌کنند:

1. `invoice.refral` در مجموعه نماینده‌های مجاز؛
2. `invoice.bottype` برابر توکن ربات اختصاصی نماینده مجاز.

برای مشاهده مشتری و سرویس، مالکیت در سمت سرور دوباره بررسی می‌شود. پارامتر `owner` نیز فقط وقتی پذیرفته می‌شود که در `scope_ids` نشست باشد.

## عملیات مالی

شارژ مشتری و انتقال اعتبار به زیرنماینده:

- با transaction و `SELECT ... FOR UPDATE` انجام می‌شود؛
- موجودی فرستنده نمی‌تواند منفی شود؛
- انتقال در `reseller_wallet_ledger` ثبت می‌شود؛
- رویداد مدیریتی در `reseller_audit_log` ذخیره می‌شود.

## محدودیت عمدی

- نماینده credential پنل‌های VPN مادر، درگاه اصلی، backup، restore، update و تنظیمات کل سیستم را نمی‌بیند.
- ایجاد/حذف دسته‌بندی پایه فقط برای ادمین است، چون جدول پایه بین همه فروشگاه‌ها مشترک است. نماینده می‌تواند نمایش دسته‌بندی را برای ربات خود کنترل کند.
- عملیات واقعی تمدید/افزایش حجم همچنان از جریان تراکنشی ربات انجام می‌شود تا کسر موجودی، API پنل و فاکتور هماهنگ بمانند؛ پنل نمایندگی credential پنل مادر را دریافت نمی‌کند.
- پرداخت‌های بدون `bottype` اختصاصی یا بدون اتصال به `id_invoice` عمداً نمایش داده نمی‌شوند تا تراکنش‌های یک مشتری در فروشگاه‌های دیگر افشا نشود.

## تست استقرار

1. یک نماینده عادی، یک سوپر و دو زیرنماینده آزمایشی بسازید.
2. بررسی کنید نماینده عادی با تغییر `owner` یا `id` نتواند داده دیگری را ببیند.
3. بررسی کنید سوپر فقط زیرنماینده مستقیم خود را می‌بیند.
4. خریدی از هر ربات انجام دهید و Dashboard/Invoice/Payment را مقایسه کنید.
5. انتقال اعتبار را با موجودی ناکافی و کافی آزمایش کنید.
6. callback پرداخت، ساخت سرویس و Cronهای اصلی را مجدداً تست کنید.
