# Red Fox 2.2.2 — بکاپ و Migration یک‌کلیکی

## حذف کادر MIGRATE

کادر اجباری تایپ `MIGRATE` کاملاً حذف شد. در مرکز مهاجرت فقط یک دکمه وجود دارد:

```text
🗄 بکاپ و بروزرسانی ساختار دیتابیس
```

پس از کلیک، یک پیام تأیید فارسی نمایش داده می‌شود. اگر تأیید شود، سیستم بدون ورودی متنی:

1. Maintenance Mode را فعال می‌کند؛
2. بکاپ رمزنگاری‌شده دیتابیس می‌گیرد؛
3. Checksum فایل‌های Migration را بررسی می‌کند؛
4. فقط Migrationهای اجرا‌نشده را اعمال می‌کند؛
5. نتیجه را در Audit Log ثبت می‌کند؛
6. Maintenance Mode را آزاد می‌کند.

## Progress واقعی Migration

Migration با AJAX اجرا می‌شود و Session Lock آزاد می‌گردد. نوار پیشرفت این مراحل را نشان می‌دهد:

- آماده‌سازی و Lock؛
- بکاپ دیتابیس؛
- بررسی هر فایل Migration؛
- اجرای فایل جاری؛
- تعداد Migration انجام‌شده از کل؛
- تکمیل یا خطا.

در Fatal Error، Marker نگهداری حذف و فایل Progress با وضعیت failed نوشته می‌شود؛ بنابراین صفحه سیاه دائمی ایجاد نمی‌شود.

## بروزرسانی

بروزرسانی همچنان به‌صورت خودکار قبل از تغییر فایل‌ها بکاپ می‌گیرد. مرحله Migration بروزرسانی نیز اکنون درصد داخلی هر Migration را به همان Progress Bar اصلی ارسال می‌کند.

## فرم‌های تأیید متنی

در پنل وب دیگر هیچ‌کدام از این ورودی‌های اجباری وجود ندارند:

```text
MIGRATE
UNSIGNED
شماره نسخه برای تأیید
Diff
```

عملیات حساس فقط با دکمه و پیام تأیید فارسی انجام می‌شوند. بررسی‌های فنی و امنیتی در Backend باقی مانده‌اند.
