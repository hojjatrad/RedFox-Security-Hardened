# راهنمای ارتقا به Red Fox 1.0.1 Security

این نسخه «فاز اول» سخت‌سازی امنیت و زیرساخت است و منطق فروش، محصولات، پنل‌های VPN و پرداخت‌ها را عمداً بازطراحی نکرده است.

## قبل از ارتقا

1. از فایل‌ها و دیتابیس بکاپ کامل بگیرید.
2. ارتقا را ابتدا روی staging انجام دهید.
3. callbackهای پرداخت، خرید، ساخت سرویس، تمدید و Cron را تست کنید.

## ترتیب نصب روی نسخه موجود

1. فایل‌های نسخه جدید را جایگزین کنید؛ `config.php` عملیاتی خودتان را کورکورانه overwrite نکنید و مقادیر DB/توکن/دامنه را به config جدید منتقل کنید.
2. فایل `migrations/001_security_hardening.sql` را یک بار روی دیتابیس اجرا کنید.
3. `table.php` را طبق روش امن فعلی پروژه یک بار اجرا کنید تا فیلدها و secretهای لازم تکمیل شوند؛ سپس دسترسی عمومی آن را ببندید.
4. ادمین با رمز قبلی وارد شود. اگر رمز قدیمی plaintext باشد، در همان ورود موفق به hash تبدیل می‌شود.
5. از داخل ربات ادمین، دستور `/token` را دوباره بگیرید و integrationهای قدیمی کارت/وبهوک را با توکن جدید به‌روزرسانی کنید. رمز پنل دیگر secret وبهوک نیست.
6. Mini App را دوباره باز کنید؛ tokenهای قدیمی عمداً منقضی شده و token جدید ۲۴ساعته صادر می‌شود.

## تغییرات امنیتی

- Hash رمز ادمین با `password_hash/password_verify` و مهاجرت خودکار رمز قدیمی.
- رمز تصادفی نصب اولیه؛ ارسال امن آن به ادمین تلگرام.
- Rate limit ورود ادمین و نماینده؛ محدودیت ۵ تلاش برای کد 2FA.
- Secure/HttpOnly/SameSite=Strict و strict-mode برای session.
- بررسی CSRF مرکزی برای تمام POSTهای پنل و پورتال؛ token یا Origin/Referer هم‌مبدأ معتبر الزامی است.
- اعتبار ۱۰ دقیقه‌ای Telegram initData و جلوگیری از replay قدیمی.
- Bearer token مینی‌اپ ۲۴ساعته و ۲۵۶بیتی.
- secret مستقل برای integrationها؛ حذف وابستگی callbackها به رمز ادمین.
- TLS verification پیش‌فرض در تمام cURLهای شناسایی‌شده.
- حذف runtime `eval`؛ قطعات legacy در فایل‌های `compiled.php` زمان build ترکیب شده‌اند.
- اصلاح نام نسخه و فاصله‌های مشکل‌ساز مسیرهای اصلی installer.

## پنل‌های دارای گواهی Self-signed

حالت امن پیش‌فرض، گواهی نامعتبر را رد می‌کند. راه درست نصب گواهی معتبر یا CA داخلی روی سرور است. فقط برای سازگاری موقت می‌توان **قبل از require شدن config.php** تعریف کرد:

```php
define('REDFOX_ALLOW_INSECURE_TLS', true);
```

این گزینه امنیت TLS همه اتصال‌های legacy را کاهش می‌دهد و برای production توصیه نمی‌شود.

## تغییر مهم integration token

توکن `/token` اکنون از `setting.integration_secret_token` می‌آید، نه از رمز ادمین. پس از مهاجرت، clientهایی که به `payment/card.php` یا `webhooks.php` متصل‌اند باید token جدید دریافت کنند.

## تست‌های انجام‌شده

- parse نحوی ۲۵۶ فایل PHP اختصاصی با tree-sitter-php: بدون خطای parse.
- `bash -n install.sh`: موفق.
- تست ایستای امنیت در `tests/security_static_test.py`: موفق.
- جست‌وجوی runtime eval و TLS verification خاموش: مورد باقی‌مانده پیدا نشد.

## محدودیت تست

به دلیل نبود دیتابیس، توکن تلگرام، پنل VPN و درگاه واقعی، تست end-to-end اجرایی انجام نشده است. استقرار مستقیم روی production بدون staging توصیه نمی‌شود.

## فاز بعدی

فاز دوم پنل یکپارچه نماینده و سوپرنماینده را با UI پنل اصلی و scope اجباری داده پیاده می‌کند: نماینده فقط داده خود، و سوپرنماینده فقط داده خود و زیرنماینده‌های مستقیمش را خواهد دید.
