# ممیزی هماهنگی ربات، پنل وب و هوش مصنوعی نماینده — 2.3.0

## اشتراک هوش مصنوعی نماینده
- قیمت و مدت هنگام درخواست Snapshot می‌شود تا تغییر بعدی تنظیمات، درخواست قبلی را عوض نکند.
- درخواست تمدید، اشتراک فعال قبلی را به حالت pending و غیرفعال تبدیل نمی‌کند.
- فعال‌سازی فقط برای توکن واقعی موجود در `botsaz` مجاز است.
- فعال‌سازی، تمدید، تاریخ دقیق، رد درخواست و غیرفعال‌سازی در Backend مرکزی و Transaction انجام می‌شود.
- تاریخ دقیق گذشته یا نامعتبر رد می‌شود.
- درخواست ردشده، اشتراک فعال قبلی را حفظ می‌کند.
- نماینده پس از فعال‌سازی، رد یا غیرفعال‌سازی اعلان تلگرام می‌گیرد.
- پنل Snapshot مبلغ/روز درخواست را نمایش می‌دهد.
- Modal بین ربات‌های مختلف کامل Reset می‌شود.

## امنیت و اجرای AI
- کلید API در HTML نمایش داده نمی‌شود و با Master Key رمزنگاری می‌شود.
- خالی گذاشتن فیلد کلید، کلید قبلی را حذف نمی‌کند.
- ربات نماینده وضعیت سراسری AI را رعایت می‌کند؛ وقتی AI اصلی خاموش است پاسخ خودکار نمی‌دهد.
- پاسخ مدل قبل از ارسال با parse mode HTML ایمن‌سازی می‌شود.
- لینک پشتیبانی با یا بدون @ درست ساخته می‌شود.
- متن راهنمای Provider دیگر عبارت اشتباه `rxCurNote` نشان نمی‌دهد.

## هماهنگی انتشار
- فایل‌های source و compiled مجدداً همگام شده‌اند.
- قالب Default و update ربات نماینده یکسان اصلاح شده‌اند.
- Installer فایل‌های اجرایی ربات‌های نماینده موجود را بدون تغییر config/token/style همگام می‌کند.
