# اصلاح نصب روی دیتابیس دارای اطلاعات — نسخه 2.2.4

- تمام خروجی‌های PDO در MigrationRunner با `fetchAll()` مصرف و با `closeCursor()` بسته می‌شوند.
- تمام Rowsetهای اضافی MySQL نیز قبل از Query بعدی تخلیه می‌شوند.
- اتصال PDO نصب‌کننده از ابتدا با `PDO::MYSQL_ATTR_USE_BUFFERED_QUERY = true` ساخته می‌شود.
- اجرای مجدد نصب روی دیتابیس نیمه‌کاره یا دارای اطلاعات، Migrationهای قبلی را رد کرده و فقط ساختارهای باقیمانده را تکمیل می‌کند.
- داده‌های کاربران، سفارش‌ها، سرویس‌ها، تنظیمات و پنل‌ها حذف نمی‌شوند.
