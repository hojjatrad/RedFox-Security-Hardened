# MYSQL EXPERT

Audit schema, keys, constraints, indexes, foreign keys, transactions, isolation/concurrency, migrations and query performance. Prevent SQL injection with prepared statements. Check monetary precision, timestamps, uniqueness, cascading behavior and race conditions. Verify queries against the real schema.
