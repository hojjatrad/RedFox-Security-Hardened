# رفع کارشناسی نهایی ظرفیت ربات نماینده — 2.3.4

شمارش invoice معیار ظرفیت واقعی پنل VPN نیست؛ ممکن است حساب‌ها خارج از ربات حذف شده باشند، وضعیت دیتابیس با پنل تفاوت داشته باشد یا سرویس‌های قدیمی باقی مانده باشند. بنابراین در ربات نماینده شمارش محلی فقط برای Log است و هرگز خرید را مسدود نمی‌کند. ساخت واقعی به Connector پنل سپرده می‌شود و فقط خطای واقعی API می‌تواند Provisioning را متوقف کند.

برای حذف مشکل نسخه Cache‌شده نیز:
- Installer همه دایرکتوری‌های واقعی vpnbot را اسکن می‌کند، نه فقط نام ساخته‌شده از دیتابیس.
- مدیریت نمایندگان با ذخیره هر نماینده، همه ربات‌های موجود را از قالب اصلاح‌شده همگام می‌کند.
- OPcache هر فایل جایگزین‌شده فوراً invalidate می‌شود.
- فایل `.redfox-template-version` در هر ربات ثبت می‌شود.
- در قالب ربات نماینده هیچ مسیر اجرایی برای پیام limitedpanelfirst/limitedpanel باقی نمانده است.
