# رفع خطای ظرفیت پنل در ربات سوپرنماینده — نسخه 2.2.7

علت‌ها:
1. در حالت تک‌پنل، ربات نماینده تعداد تمام فاکتورهای همه پنل‌ها را می‌شمرد، نه فقط پنل انتخاب‌شده.
2. برای شمارش از `PDOStatement::rowCount()` روی SELECT استفاده شده بود که قابل اتکا نیست.
3. مقدارهای خالی، صفر و املای قدیمی `unlimted` به‌درستی به‌عنوان نامحدود تشخیص داده نمی‌شدند.
4. در ربات اصلی شرط وضعیت فاکتور از نظر منطقی غیرممکن بود (`active AND expired`).

اصلاحات:
- شمارش با `COUNT(*)` و فقط برای `Service_location` همان پنل انجام می‌شود.
- ظرفیت نامعتبر، خالی، صفر، منفی، `unlimted` و `نامحدود` باعث مسدود شدن فروش نمی‌شود.
- Migration 018 مقدارهای قدیمی را به `unlimited` تبدیل می‌کند.
- فرم پنل فقط `unlimited` یا عدد مثبت می‌پذیرد.
- فایل‌های اجرایی ربات‌های نماینده موجود هنگام نصب دوباره همگام می‌شوند و config/token/style آن‌ها حفظ می‌شود.
- رخداد واقعی پرشدن ظرفیت با نام پنل، مقدار مصرف و سقف در error log ثبت می‌شود.
