# ممیزی کامل Index و نصب دیتابیس — نسخه 2.2.6

این نسخه تمام محل‌های ساخت Primary Key، Unique Key و Index در `table.php` و Migrationها را بررسی و برای `utf8mb4` اصلاح می‌کند.

- ستون‌های شناسه قدیمی با تعریف‌های 500/600 کاراکتری فقط پس از بررسی طول واقعی داده، بدون حذف اطلاعات به 191 کاهش پیدا می‌کنند.
- اگر داده‌ای واقعاً بلندتر از حد امن باشد، نصب‌کننده نام دقیق جدول و ستون را اعلام می‌کند و آن را truncate نمی‌کند.
- Indexهای ترکیبی از Prefixهای حداکثر 63/75/100/120 استفاده می‌کنند.
- شناسه‌های فنی مناسب در جدول‌های جدید با charset ASCII ذخیره می‌شوند.
- محدودیت‌های قدیمی 767 بایت و محدودیت جدید 3072 بایت InnoDB هر دو پوشش داده شده‌اند.
- مرحله دقیق خطا در نصب‌کننده نمایش داده می‌شود: تعمیر دیتابیس قدیمی، جداول پایه یا Migration.
- Checksum تمام Migrationهای تاریخی اصلاح‌شده با نسخه‌های قبلی سازگار است.
