<?php
require_once __DIR__.'/../config.php';require_once __DIR__.'/lib/Portal.php';require_once __DIR__.'/lib/Layout.php';$ctx=rxp_context($pdo);rxp_require_perm($ctx,'reports');[$ph,$params]=rxp_in($ctx['scope_ids']);$q=$pdo->prepare("SELECT * FROM reseller_audit_log WHERE reseller_id IN ($ph) ORDER BY id DESC LIMIT 500");try{$q->execute($params);$rows=$q->fetchAll(PDO::FETCH_ASSOC);}catch(Throwable $e){$rows=[];}rxp_layout_start($ctx,'لاگ فعالیت','audit.php');?>
<h1 class="rxp-title">لاگ فعالیت</h1><div class="rxp-sub">عملیات حساس پنل شما و زیرنمایندگان مستقیم</div><div class="rxp-card"><div class="rxp-table-wrap"><table class="rxp-table"><tr><th>زمان</th><th>عامل</th><th>عملیات</th><th>موجودیت</th><th>IP</th></tr><?php foreach($rows as $r):?><tr><td><?=date('Y/m/d H:i',(int)$r['created_at'])?></td><td><?=rxp_h($r['reseller_id'])?></td><td><?=rxp_h($r['action'])?></td><td><?=rxp_h($r['entity'])?></td><td><?=rxp_h($r['ip'])?></td></tr><?php endforeach;?></table></div></div><?php rxp_layout_end();
