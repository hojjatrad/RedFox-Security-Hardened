# تکمیل پلتفرم Red Fox 2.4.0

## رابط فارسی و تاریخ
- اسکریپت مشترک Persian Date به Header پنل اصلی و Layout نمایندگان متصل شد.
- تمام inputهای date و datetime-local به‌صورت خودکار برچسب شمسی فارسی می‌گیرند.
- نمایش تاریخ تسویه و بخش‌های اصلاح‌شده با jdate انجام می‌شود.
- گزینه‌های انگلیسی عملیات سرویس به برچسب فارسی تبدیل شدند.

## محصولات
- ساخت و ویرایش محصول در پنل اصلی فقط با select دسته‌بندی انجام می‌شود.
- پنل نمایندگان از قبل select داشت و اعتبار دسته در Backend کنترل می‌شود.

## اعتبار نماینده
- خرید در ربات نماینده بدون اعتبار کافی متوقف و پیام روشن برای مشتری نمایش داده می‌شود.
- نماینده و مدیر اصلی هشدار کمبود موجودی می‌گیرند.
- حد هشدار از مدیریت نمایندگان قابل تنظیم است.
- هشدار با فاصله زمانی کنترل می‌شود تا Spam ایجاد نشود.

## سرویس و عملیات
- صفحه سرویس‌ها توضیح می‌دهد که service_other شامل تمدید، حجم/زمان اضافه، انتقال و تغییر لوکیشن است.
- حالت خالی صفحه به‌صورت فارسی توضیح داده می‌شود.
- فیلتر و وضعیت عملیات نمایندگان فارسی شد.

## تسویه
- رمزگشایی شبا در try/catch امن انجام می‌شود؛ نبود Master Key دیگر صفحه را 500 نمی‌کند.
- تنظیمات پیش‌فرض در صورت نبود Row اعمال می‌شوند.
- تاریخ و وضعیت درخواست‌ها فارسی شده است.

## اعلان وب
- Header تعداد رسیدهای منتظر، درخواست‌های نمایندگی و تسویه‌های منتظر را به‌صورت آلارم قرمز نمایش می‌دهد.

## نرم‌افزارها
- لینک‌های رسمی v2rayNG، Hiddify Next، NekoBox، V2Box، Streisand و Nekoray به‌صورت پیش‌فرض Seed می‌شوند.
- افزودن، ویرایش و حذف در apps و applinks پشتیبانی می‌شود.

## ایموجی
- فهرست انتخابی ایموجی‌های پرکاربرد اضافه شد.
- شناسه Custom Emoji باید واقعی و متعلق به Telegram باشد؛ شناسه جعلی Seed نمی‌شود.

## هوش مصنوعی
- راهنمای دقیق OpenRouter و Groq در صفحه تنظیمات اضافه شد.
- برای کارکرد باید status=on و key=SET در Debug نشان داده شود و مسیر پشتیبانی ربات استفاده شود.
