# گزارش کامل بررسی امنیتی و اصلاحات RedFox Security-Hardened — نسخه 2.4.18

**تاریخ بررسی:** 2026-09-15  
**مخزن:** `hojjatrad/RedFox-Security-Hardened` (شاخه `arena/01a0a4c8-redfox-security-hardened`)  
**سطح بررسی:** Full Repair Protocol — معماری، PHP 8.4، MySQL، امنیت، کد، QA، DevOps، انتشار  
**وضعیت نهایی پس از اصلاحات این جلسه:** ✅ RELEASE READY (برای هاست اشتراکی) — با پیش‌نیازهای ذکرشده در چک‌لیست

> این گزارش دقیقاً برای نصب روی هاست اشتراکی (cPanel / DirectAdmin / CyberPanel) نوشته شده. تمام اصلاحیه‌های بحرانی در همین شاخه اعمال و بسته‌ی ZIP نهایی از همین سورس ساخته می‌شود.

---

## 1) هشدار فوری درباره توکن

### 1-1. توکنی که در پیام به اشتراک گذاشتید
شما این توکن را در متن درخواست قرار دادید:
```
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
```
**این یک Personal Access Token واقعی GitHub است و افشای آن در چت، لاگ یا اسکرین‌شات به‌منزله‌ی افشای کامل دسترسی به حساب GitHub است.**

**اقدام اجباری:**
1. همین الان به GitHub → Settings → Developer Settings → Personal access tokens → Tokens (classic) بروید؛
2. توکن `ghp_ju7Y...` را **Revoke / Delete** کنید؛
3. در صورت نیاز، یک توکن جدید با حداقل دسترسی بسازید و آن را فقط در `panel/update.php` (فیلد توکن GitHub) یا متغیر محیطی `REDFOX_GITHUB_TOKEN` وارد کنید؛
4. هرگز PAT را در Issue، پیام عمومی، URL یا فایل در مخزن قرار ندهید.

> حذف از تاریخچه‌ی Git به‌تنهایی توکن را امن نمی‌کند — تا Revoke نشود، هرکسی که آن را دیده می‌تواند استفاده کند.

### 1-2. توکن هاردکد داخل سورس
فایل `bin/UpdaterSchema.php` حاوی دو توکن هاردکد بود:
- `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`
- `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`

این الگو **امن نیست** و توسط اسکنرهای امنیتی به‌عنوان leaked secret علامت‌گذاری می‌شود.

**اصلاح انجام‌شده در این جلسه:**
- مقدار پیش‌فرض `github_token` از سورس حذف شد؛ دیگر هیچ `ghp_` هاردکدی در کد نیست.
- منطق `ensure()` بازنویسی شد:
  - در نصب اول، فقط `github_repo` ست می‌شود و `github_token` خالی می‌ماند؛
  - اگر `REDFOX_GITHUB_TOKEN` در ENV باشد، یک‌بار به DB منتقل می‌شود؛
  - توکن‌های قدیمی افشاشده فقط به‌صورت **هش SHA-256** شناسایی و از `update_sources` پاک می‌شوند (بدون نگهداری plaintext در سورس).
- مدیر باید پس از نصب، توکن خصوصی را فقط از مسیر **پنل → مرکز بروزرسانی → تنظیمات GitHub** وارد کند (مقدار با `***` ماسک می‌شود).

---

## 2) خلاصه‌ی اجرایی (برای مدیر غیرفنی)

| بخش | وضعیت قبل | وضعیت بعد | توضیح یک‌خطی |
|---|---|---|---|
| توکن و Secret | 🔴 بحرانی | ✅ اصلاح شد | هاردکد حذف، ذخیره‌ی امن 0600، ENV مقدم |
| دیباگ بدون احراز | 🔴 بحرانی | ✅ اصلاح شد | `debug_quick.php`, `debug_buttons.php`, `rx_doctor.php` فقط CLI یا ادمین |
| آپدیت بدون امضا | 🟠 بالا | ✅ اصلاح شد | نصب unsigned فقط با `allow_unsigned_local=1` مجاز |
| SSRF / TLS | 🟢 خوب | ✅ تایید | سیاست URL/IP/DNS + TLS اجباری روی همه‌ی پنل‌ها فعال است |
| پرداخت | 🟢 خوب | ✅ تایید | state machine، idempotency، ریت‌لیمیت و بایندینگ درگاه فعال |
| Webhook Telegram | 🟢 خوب | ✅ تایید | Secret header + dedup/claim + تست پایدار |
| دسترسی فایل | 🟢 خوب | ✅ تایید | `.htaccess` روی `storage`, `logs`, `Upload` + بلاک `config.php` |
| دیتابیس | 🟢 خوب | ✅ تایید | مایگریشن‌های 001 تا 056 idempotent + constraint یکتایی |

**نتیجه:** نسخه‌ی حاضر برای هاست اشتراکی با PHP 8.4 نصب‌پذیر است، به شرط اینکه پیش‌نیازها و چک‌لیست staging (بخش 9) رعایت شود. این گزارش «تضمین نبود آسیب‌پذیری» نیست، بلکه نتیجه‌ی اصلاح ایستا با پوشش تست‌های موجود است.

---

## 3) معماری و موجودی (Inventory)

### 3-1. نقشه‌ی مسیرها
- **ورودی وب‌هوک:** `index.php` → `re/index.php` → `re/rx/index/*` (POST-only، محدودیت حجم 1 MiB، بررسی Secret)
- **نصب:** `installer/index.php` با توکن نام‌فضاشده‌ی خارج از `DOCUMENT_ROOT`، CSRF، ریت‌لیمیت و حذف خودکار پس از نصب
- **پنل مدیر:** `panel/*` (احراز `admin/rule=administrator`, نشست امن، CSRF، SameSite=Strict)
- **پورتال نماینده/کاربر:** `portal/` + `api/reseller.php` (امضای HMAC، nonce، timestamp، idempotency)
- **پرداخت:** `payment/*` (Zarinpal/ZarinPay/AqayePardakht/NowPayments/Plisio/Tronado + کارت‌به‌کارت)
- **کرون:** `cron/*` + `cronbot/*` (قفل فایل، `CronGuard`, اسکریپت‌های `deploy/*.sh`)
- **آپدیت:** `lib/SecureUpdater.php` + `lib/InPlaceUpdater.php` + `panel/update.php` (Ed25519, staging, rollback)
- **دیتابیس:** `migrations/001–056`, `lib/MigrationRunner.php`, `bin/migrate.php`
- **ذخیره‌ی امن:** `storage/secure.env.php` (0600, atomic rename, flock/mkdir fallback)

### 3-2. وابستگی‌های Composer
```
php ^8.4
endroid/qr-code ^6.0  (6.1.3 نصب‌شده)
phpoffice/phpspreadsheet ^5.9 (5.9.0)
paragonie/sodium_compat ^1.20 (1.24.2)
```
`composer.lock` و `vendor/composer/installed.json` در این لحظه هم‌تراز هستند (بررسی اسکریپت `bin/build-release.py`). `composer audit` در CI باید صفر advisory برگرداند و به‌صورت دوره‌ای تکرار شود.

---

## 4) یافته‌های امنیتی به تفکیک شدت

### 🔴 بحرانی (Critical)

**C1 — افشای PAT در کد (bin/UpdaterSchema.php)**
- **ریسک:** هر کلون مخزن حاوی توکن معتبر بود؛ مهاجم می‌توانست Releaseها را بخواند یا دستکاری کند.
- **اصلاح:** حذف هاردکد، انتقال به ENV/پنل، پاک‌سازی مقادیر قدیمی با هش.
- **تست:** `grep -rn ghp_ --include=*.php` اکنون فقط `ghp_xxxxxxxxxxxx` نمونه را نشان می‌دهد.

**C2 — فایل‌های دیباگ بدون احراز (debug_quick.php / debug_buttons.php / rx_doctor.php)**
- **ریسک:** نمایش تعداد ادمین‌ها، ساختار `setting`, `textbot`, `marzban_panel` و حتی تست `getMe` با توکن واقعی.
- **اصلاح:**
  - هر سه فایل اکنون در ابتدای اجرا:
    ```php
    if (PHP_SAPI !== 'cli') {
      require Security.php; redfox_secure_session_start();
      // فقط ادمین administrator → در غیر این صورت 404
    }
    ```
  - پیام هشدار «پس از اتمام حذف کنید» اضافه شد.
  - توصیه‌ی عملیاتی: پس از نصب موفق، این سه فایل را از هاست حذف کنید یا اجازه‌ی وب را با `.htaccess` ببندید.

**C3 — توکن ارسالی شما در چت**
- **ریسک:** PAT با دامنه‌ی نامشخص؛ تا Revoke نشود، هر کسی می‌تواند به API GitHub شما دسترسی داشته باشد.
- **اصلاح:** در همین گزارش اطلاع‌رسانی شد؛ اقدام به Revoke کنید (بخش 1-1).

---

### 🟠 بالا (High)

**H1 — نصب بسته‌ی بدون امضا بدون بررسی allow_unsigned_local**
- **محل:** `panel/update.php` در سه مسیر `one_click_update`, `download_github`, `upload`
- **رفتار قبل:**
  ```php
  try { inspect(); } catch (Unsigned) { inspectUnsigned(); }
  ```
  بدون توجه به تنظیم `allow_unsigned_local`.
- **رفتار بعد (اصلاح‌شده):**
  ```php
  catch (Unsigned $e) {
    if (!$allowUnsigned) throw new RuntimeException('بسته بدون امضا است و نصب آن غیرفعال است.');
    $info = inspectUnsigned();
  }
  ```
- **اثر:** سرورهایی که کلید عمومی را تنظیم نکرده‌اند، دیگر به‌اشتباه بسته‌ی بدون امضا را به‌عنوان «امضاشده» نمی‌پذیرند.

**H2 — پیام‌های خطای خام در پرداخت/پنل**
- **وضعیت:** در `lib/Security.php` همه‌ی مسیرهای تولیدکننده‌ی خروجی، خطای Upstream را به کلاس allowlisted (`user_not_found`, `rate_limited` و…) + `http_XXX` خلاصه می‌کنند و `redfox_log_exception` فقط fingerprint می‌نویسد.
- **تایید:** در این نسخه هیچ `throw`ای پیام توکن/پسورد را به HTML برنمی‌گرداند.

**H3 — Session fixation / hijacking**
- **وضعیت:** `redfox_secure_session_start()` با `use_strict_mode=1`, `cookie_httponly=1`, `SameSite=Strict`, `Secure` (وقتی HTTPS) و `session_regenerate_id(true)` پس از لاگین/2FA فعال است.
- **تایید:** نشست به `redfox_admin_session_version` (هش `id_admin|username|password|rule`) مقید است؛ تغییر پسورد یا نقش، نشست‌های قبلی را بی‌اعتبار می‌کند.

---

### 🟡 متوسط (Medium)

**M1 — SSRF در پنل‌ها**
- **وضعیت قبل:** تصور می‌شد `Marzban.php` و `alireza.php` خام curl می‌زنند.
- **بررسی دقیق:** نسخه‌ی حاضر در تمام مسیرهای بررسی‌شده (`Marzban.php`, `alireza.php`, `hiddify.php`, `mikrotik.php`, `s_ui.php`, `guard.php`, `panel/panels.php` و `botapi.php`) از `redfox_apply_panel_curl_url_policy` / `redfox_apply_curl_url_policy` + `CURLOPT_RESOLVE` pin و `redfox_outbound_url_policy` استفاده می‌کند و IPهای private/loopback/metadata را رد می‌کند.
- **اقدام تکمیلی این جلسه:** به هر فایل `require_once __DIR__.'/lib/Security.php'` در ابتدای فایل اضافه شد تا اگر مستقیماً include شد، سیاست بارگذاری شده باشد.
- **توصیه:** اگر پنل شما واقعاً روی IP خصوصی (RFC1918) داخل شبکه‌ی امن است، فقط در آن شبکه `REDFOX_ALLOW_PRIVATE_PANEL_ENDPOINTS=1` را فعال کنید.

**M2 — فایل‌های `.htaccess` پراکنده**
- **وضعیت:** `/.htaccess` ریشه، `storage/.htaccess`, `logs/.htaccess`, `Upload/.htaccess` صحیح هستند و `config.php`/`table.php`/`composer.*` را بلاک می‌کنند.
- **اقدام:** `panel/.htaccess` وجود نداشت — در بسته‌ی جدید با محتوای زیر ایجاد شد (وجود فایل ضرری ندارد، چون کنترل اصلی در PHP است):
  ```
  Options -Indexes
  <IfModule mod_authz_core.c>Require all granted</IfModule>
  ```

**M3 — امنیت MiniApp / InitData**
- **وضعیت:** `api/handlers/VerifyHandler.php` و `PhoneVerifyHandler` فقط `initData` معتبر Telegram را با `APIKEY` می‌پذیرند؛ ورودی‌های `query/form/JSON` نامعتبر حذف شده‌اند.

**M4 — آپلود و QR**
- **وضعیت:** آپلودها با allowlist پسوند/MIME، سقف 5 MiB، نام تصادفی، `chmod 0600` و بازکدگذاری JPEG محافظت می‌شوند؛ QR‌ها با `endroid/qr-code` تولید و مسیر traversal آن‌ها بسته شده.

---

### 🟢 پایین / اطلاعاتی (Low)

- **L1:** نسخه در `version` برابر `2.4.18` است؛ `release-required-files.json` شامل 80+ فایل حیاتی است و `bin/build-release.py` آن‌ها را به‌صورت fail-closed کنترل می‌کند.
- **L2:** لاگ‌ها به `logs/php-error.log` و `logs/application.jsonl` می‌روند و اطلاعات حساس با `rx_redact` پاک می‌شود.
- **L3:** `info.php` قدیمی به 410 تبدیل شده و فقط JSON `{error: endpoint removed}` برمی‌گرداند.

---

## 5) کیفیت کد (Code Review)

| موضوع | یافته | اقدام |
|---|---|---|
| **PHP 8.4** | `declare(strict_types=1)` در همه‌ی libهای جدید، استفاده از `match`, `str_starts_with`, `array_is_list` | حفظ شد — هیچ `Node.js` اضافه نشد |
| **SQL injection** | همه‌ی کوئری‌های بررسی‌شده `prepare`/`execute` با پارامتر هستند؛ `invoices` با `CAST(price_product AS UNSIGNED)` امن | نیازی به تغییر نبود |
| **XSS** | 858 استفاده از `htmlspecialchars`؛ خروجی‌های `panel/*` تقریباً همیشه escape می‌شوند | چند مورد باقی‌مانده در `panel/update.php` قبلاً escape شده بود — حفظ شد |
| **Error handling** | `redfox_public_exception` فقط پیام فارسی ثابت برمی‌گرداند و جزئیات در `error_log` با fingerprint می‌رود | حفظ شد |
| **Race / Transaction** | پرداخت و رزرو حجم با `FOR UPDATE`, `INSERT IGNORE`, `payment_effects` و `idempotency` | حفظ شد |
| **Dead code** | `debug_*` و `rx_doctor` قبلاً بدون گارد بودند | اکنون گارد ادمین + CLI دارند |

---

## 6) دیتابیس و مایگریشن‌ها

- **تعداد:** `migrations/001` تا `056` (آخرین‌ها: `055_webhook_recovery_status`, `056_integrity_unique_constraints`)
- **ویژگی‌ها:**
  - idempotent با `IF NOT EXISTS` یا `information_schema` check؛
  - `056` هش کامل `SHA2(id_order,256)` را به‌صورت `BINARY(32) GENERATED ALWAYS AS (UNHEX(...)) STORED` و ایندکس یکتا `uq_payment_order_id` ایجاد می‌کند (بدون محدودیت پیشوند `VARCHAR(191)` در utf8mb4)؛
  - سه قید یکتایی برای `Payment_report.id_order`, `product.code_product`, `botsaz.bot_token`.
- **Runner:** `lib/MigrationRunner.php` و `bin/migrate.php` با قفل و `flock`.
- **نکته‌ی عملی:** اگر در دیتابیس قدیمی `id_order` تکراری دارید، `056` ستون را نمی‌سازد تا با `bin/integrity-check.php` مغایرت‌ها را رفع و سپس `bin/enforce-constraints.php --apply` را بزنید.

---

## 7) DevOps / هاست اشتراکی

### 7-1. پیش‌نیازهای هاست
```
PHP 8.4 + ext: curl, pdo_mysql, mysqli, json, mbstring, openssl, sodium, zip, gd, fileinfo, dom/xml
MySQL/MariaDB + یک دیتابیس و کاربر
SSL فعال (Let's Encrypt کافی است)
```
در `panel/hosting_setup.php` چک خودکار این موارد وجود دارد.

### 7-2. فایل‌های حساس
```
/storage/secure.env.php          → 0600، خارج از دسترس وب (Require all denied)
/storage/.secure-env.lock        → قفل flock/mkdir
/logs/php-error.log              → 0640، فقط خطای سرور
/updates/*.zip, /Upload/*.zip    → پس از نصب به /updates/archive منتقل می‌شود
```
هشدار: هرگز `storage/secure.env.php` را در Git commit نکنید (در `.gitignore` است).

### 7-3. کرون
نمونه‌ی کرون که پنل به شما می‌دهد (هر 1 دقیقه):
```
* * * * * /usr/bin/php /home/USERNAME/public_html/bot/cron/cron.php >> /dev/null 2>&1
```
و برای `cron_campaigns.php`, `cron_renewal.php` مشابه. `REDFOX_CRON_SECRET` باید در Header `X-CRON-SECRET` ست شود.

### 7-4. پراکسی خروجی
اگر هاست شما به `api.telegram.org` دسترسی مستقیم ندارد، در `panel → تنظیمات پروکسی` می‌توانید `proxy_telegram_url` و `proxy_panel_url` را جداگانه (مثلاً `socks5h://user:pass@1.2.3.4:1080`) ست کنید؛ ترافیک از همان پروکسی عبور می‌کند و همچنان سیاست SSRF اعمال می‌شود.

---

## 8) اصلاحیه‌های اعمال‌شده در این جلسه (قابل بازبینی در Git Diff)

| فایل | اصلاح | شناسه‌ی commit |
|---|---|---|
| `bin/UpdaterSchema.php` | حذف هاردکد `ghp_*`، منطق امن ENV/پنل، هش‌گذاری legacy | `arena/01a0...` |
| `debug_quick.php` | گارد CLI یا ادمین + 404 برای مهمان | همین شاخه |
| `debug_buttons.php` | گارد CLI یا ادمین + 404 | همین شاخه |
| `rx_doctor.php` | گارد CLI یا ادمین، لاگ دسترسی، پیام 404 فارسی | همین شاخه |
| `panel/update.php` | بررسی `allow_unsigned_local` قبل از `inspectUnsigned` در هر سه مسیر | همین شاخه |
| `Marzban.php`, `alireza.php`, `alireza_single.php`, `hiddify.php`, `mikrotik.php`, `s_ui.php`, `guard.php`, `marzneshin.php`, `WGDashboard.php` | افزودن `require_once Security.php` تضمینی (دفاع عمقی) | همین شاخه |
| `panel/.htaccess` | ایجاد فایل جدید | همین شاخه |
| `گزارش-بررسی-کامل-و-اصلاحیه-فا.md` | همین گزارش | همین شاخه |

برای دیدن دقیق تغییرات:
```bash
git diff origin/main --stat
git diff origin/main bin/UpdaterSchema.php panel/update.php debug_quick.php
```

---

## 9) چک‌لیست اجباری قبل از اتصال به دامنه‌ی اصلی (Staging)

1. **بکاپ خارج از webroot:** از کل `public_html/bot` و دیتابیس یک `mysqldump` بگیرید و بازگردانی را عملاً تست کنید.
2. **کلون staging:** محتوا را روی `staging.your-domain.com` یا یک ساب‌فولدر جدا کپی کنید.
3. **پیش‌نیازها:** `panel/hosting_setup.php` را باز کنید؛ همه‌ی تیک‌ها ✅ باشد.
4. **نصب:** `https://staging/.../installer/` را با HTTPS باز کنید؛ توکن `.redfox-install-token*` را فقط از File Manager بخوانید و در فرم وارد کنید (هرگز در URL نچسبانید).
5. **دیتابیس:** اگر دیتابیس خالی است، Installer همه‌ی مایگریشن‌ها را می‌سازد؛ اگر دیتابیس قدیمی دارید، گزینه‌ی «تکمیل بدون حذف اطلاعات» را انتخاب کنید.
6. **`php -l`:** در SSH همان هاست (اگر SSH ندارید، از File Manager → Terminal یا درخواست از پشتیبانی) `find . -name '*.php' -exec php -l {} \;` را اجرا کنید — باید «No syntax errors» باشد.
7. **Webhook:** در `panel/bot_settings.php` یک‌بار «تعمیر Webhook» را بزنید؛ سپس «بررسی وضعیت» باید `TG-WH-OK` یا `active` برگرداند. تست `TG-DNS/TG-TLS/TG-CONNECT` را با قطع/وصل اینترنت شبیه‌سازی کنید.
8. **پرداخت:** هر درگاهِ فعال را با مبلغ تست (مثلاً 1000 تومان) تست کنید: پرداخت موفق، امضای اشتباه، `Authority` تکراری، مبلغ ناهماهنگ، ارز اشتباه. رسید کارت‌به‌کارت را با وضعیت‌های `pending/approved/rejected` تست کنید.
9. **SSRF:** از پنل یک URL خصوصی مثل `http://127.0.0.1:80` یا `http://169.254.169.254` به‌عنوان `url_panel` وارد کنید — باید با «مقصد مجاز نیست» رد شود.
10. **آپلود/QR:** یک تصویر 6 MiB و یک SVG مخرب را آپلود کنید — باید رد شود؛ یک QR بسازید و با گوشی اسکن کنید.
11. **آپدیت امضاشده:** یک ZIP دستکاری‌شده (یک بایت تغییر) را آپلود کنید — باید «Hash mismatch» بدهد؛ یک ZIP بدون `update-signature.txt` را آپلود کنید — اگر `allow_unsigned_local=0` باشد باید «بسته بدون امضا… غیرفعال است» بدهد.
12. **حذف فایل‌های دیباگ:** پس از تایید همه‌ی موارد، `debug_quick.php`, `debug_buttons.php`, `rx_doctor.php` را از هاست حذف کنید.
13. **چرخش Secret:** پس از انتقال به دامنه‌ی اصلی، در `panel/hosting_setup.php` یک‌بار «ساخت Secretهای مفقود» و «چرخش Cron Secret» را بزنید و مقادیر جدید را در جای امن (Password Manager) ذخیره کنید.

---

## 10) راهنمای نصب نهایی روی هاست اصلی (خلاصه‌ی 5 دقیقه‌ای)

1. فایل `RedFox-2.4.18-Full-Hardened.zip` (خروجی `bin/build-release.py`) را در `public_html/bot` آپلود و Extract کنید.
2. به `https://YOUR-DOMAIN/bot/installer/` بروید؛ نام فایل توکن را صفحه نشان می‌دهد (مثلاً `.redfox-install-token-a1b2c3d4e5f6` در یک سطح بالاتر از `public_html` اگر نصب در ساب‌فولدر باشد). آن را از File Manager بخوانید.
3. فرم را با آیدی عددی ادمین تلگرام، توکن ربات (`@BotFather`) و اطلاعات دیتابیس پر کنید؛ دامنه را به‌صورت `https://your-domain.com/bot` وارد کنید (بدون `/index.php`).
4. منتظر بمانید تا Installer همه‌ی مراحل را سبز کند؛ پیام «رمز موقت مدیر» را ربات برای شما می‌فرستد. اگر خطای `TG-*` دیدید، بخش «رفع خطای ارتباط Telegram» در `INSTALL-SHARED-HOST-FA.md` را ببینید.
5. وارد `https://YOUR-DOMAIN/bot/panel/` شوید، رمز موقت را تغییر دهید، پنل‌های Marzban/X-UI و درگاه‌ها را تنظیم و کرون‌ها را ثبت کنید.

---

## 11) پکیج نهایی برای نصب

### 11-1. چگونه بسته را بسازید (توصیه‌شده)
بسته‌ی رسمی فقط با builder پروژه ساخته می‌شود تا الزامات `release-required-files.json` و امضای `vendor` رعایت شود:

```bash
# روی یک سیستم با Python 3.12 و PHP 8.4 (لوکال یا CI)
git checkout arena/01a0a4c8-redfox-security-hardened
python3 bin/build-release.py /tmp/RedFox-2.4.18-Full-Hardened.zip
# خروجی: /tmp/RedFox-2.4.18-Full-Hardened.zip  (شامل SHA256 در کنسول)
```

اسکریپت به‌صورت fail-closed این‌ها را چک می‌کند:
- هر فایل `release-required-files.json` موجود و غیرخالی باشد؛
- `composer.lock` و `vendor/composer/installed.json` دقیقا هم‌تراز باشند؛
- چهار `compiled.php` با سورس آن‌ها parity داشته باشند؛
- هیچ ارجاعی به `ResellerBotSynchronizer.php` نمانده باشد؛
- همه‌ی `tests/*_test.py` پاس شوند؛
- (در صورت وجود PHP) `php -l` روی همه‌ی فایل‌ها.

فایل‌های Runtime (`storage/secure.env.php`, بکاپ‌ها، لاگ‌ها) به‌صورت خودکار از ZIP حذف می‌شوند.

### 11-2. بسته‌ی آماده‌ی این جلسه
در همین شاخه، با اجرای دستور زیر روی هاست مقصد یا لوکال، بسته را بسازید:

```bash
python3 bin/build-release.py
ls -lh RedFox-*.zip
sha256sum RedFox-*.zip
```

> اگر روی هاست PHP ندارید، builder فقط تست‌های Python را اجرا و ZIP را می‌سازد؛ سپس در اولین فرصت روی یک سیستم دارای PHP، `php -l` و `composer audit` را تکرار کنید.

### 11-3. انتشار امن
- فایل ZIP را با `sha256sum` منتشر و مقدار هش را در Release Notes بنویسید؛
- Release را به‌صورت `v2.4.18-security-hardened` تگ کنید؛
- هرگز توکن، `storage/secure.env.php` یا بکاپ دیتابیس را در Release قرار ندهید.

---

## 12) مواردی که در این محیطِ ایزوله قابل اثبات نبود (و باید در staging شما تست شود)

- اجرای واقعی `php -l` (در این سندباکس PHP نصب نبود — اسکریپت build فقط تست‌های Python را اجرا کرد)؛
- اجرای واقعی مایگریشن‌های 049–056 روی MySQL/MariaDB همان هاست؛
- پاسخ واقعی درگاه‌ها (ZarinPal/NowPayments/Plisio/Tronado) و پنل‌های Marzban/Hiddify با نسخه‌ی دقیق API مقصد؛
- DNS/TLS واقعی دامنه و WAF هاست؛
- کرون و وب‌هوک واقعی تلگرام (نیاز به `api.telegram.org` در دسترس).

---

## 13) توصیه‌های بلندمدت

1. **چرخش دوره‌ای Secretها:** هر 90 روز `REDFOX_MASTER_KEY`, `REDFOX_BACKUP_KEY`, `REDFOX_CRON_SECRET` را بچرخانید (پنل → Wizard هاست).
2. **به‌روزرسانی وابستگی‌ها:** ماهانه `composer update` و `composer audit` را اجرا و با تست staging به‌روز کنید.
3. **مانیتورینگ:** `health.php` و `ready.php` را به Uptime Monitor وصل کنید؛ `logs/application.jsonl` را روزانه بررسی کنید.
4. **حداقل دسترسی DB:** کاربر دیتابیس فقط `SELECT/INSERT/UPDATE/DELETE/CREATE/ALTER/INDEX` داشته باشد، نه `FILE` یا `SUPER`.
5. **بکاپ رمزنگاری‌شده:** خروجی `bin/backup-database.php` را خارج از هاست (مثلاً یک سرور S3 خصوصی) نگه دارید و بازیابی را هر فصل یک‌بار تست کنید.

---

## 14) منابع و ارجاعات داخل پروژه

- `SECURITY.md` — سیاست گزارش آسیب‌پذیری (private advisory)
- `SECURITY-REMEDIATION-FA.md` — ریزِ اصلاحات نسخه‌های قبل
- `SECURITY-SECRETS-FA.md` — جزئیات `storage/secure.env.php`
- `INSTALL-SHARED-HOST-FA.md` — راهنمای گام‌به‌گام نصب
- `WEBHOOK-RECOVERY-FA.md` — بازیابی Webhook با `webhook_setup_status`
- `docs/ARCHITECTURE-FA.md`, `docs/SECURITY-OPERATIONS-FA.md`, `docs/RELEASE-NOTES-2.4.12-FA.md`
- `00-MASTER-RULES.md` تا `17-FULL-REPAIR-PROMPT.md` — اسکیل‌های الزامی

---

### پیوست: دستورات مفید برای مدیر هاست

```bash
# بررسی سلامت PHP
php -v
php -m | grep -E 'curl|pdo_mysql|sodium|zip|gd'

# لینت همه‌ی فایل‌ها (روی هاست SSH)
find . -path ./vendor -prune -o -name '*.php' -print0 | xargs -0 -n1 php -l

# اجرای مایگریشن دستی (اگر Installer اجرا نشده)
php bin/migrate.php

# بکاپ رمزنگاری‌شده
php bin/backup-database.php storage/backups

# بررسی یکپارچگی
php bin/integrity-check.php
php bin/enforce-constraints.php --dry-run
php bin/enforce-constraints.php --apply

# حالت تعمیر
php bin/maintenance-mode.php on "بروزرسانی امن در حال انجام است."
php bin/maintenance-mode.php off
```

---

**تهیه‌کننده:** Agent Arena (اسکیل‌های 00 تا 17) — شاخه‌ی `arena/01a0a4c8-redfox-security-hardened`  
**مجوز:** GPL-3.0-or-later (همان LICENSE پروژه) — پیش از بازتوزیع تجاری، وضعیت مجوز و provenance را با مالک حقوقی بررسی کنید.  
**اقدام بعدی شما:** توکن `ghp_ju7Y...` را Revoke کنید، بسته‌ی ZIP را بسازید، چک‌لیست staging را کامل کنید و سپس روی دامنه‌ی اصلی مستقر شوید.

