# گزارش ممیزی Red Fox 2.3.0

## محدوده بررسی

- هماهنگی پنل وب، ربات اصلی و ربات‌های نماینده
- قالب Default و Update ربات نماینده
- فایل‌های Source و Compiled
- پنل‌ها و Provisioning
- ظرفیت پنل و فروش
- Scope نماینده و سوپرنماینده
- اشتراک و اجرای هوش مصنوعی نماینده
- تنظیم قیمت، درخواست، تأیید، تمدید، رد و غیرفعال‌سازی AI
- امنیت کلید API هوش مصنوعی
- Migration، Installer و همگام‌سازی ربات‌های ساخته‌شده
- کنترل‌های امنیتی Static شامل CSRF، TLS و eval

## اصلاحات شاخص

1. منطق ظرفیت پنل فقط همان Service Location را با COUNT واقعی محاسبه می‌کند.
2. پنل وب و جریان ربات برای Passargard/Marzban/Marzneshin و Inboundهای لازم هماهنگ است.
3. ربات‌های نماینده موجود هنگام Installer از قالب Update همگام می‌شوند؛ config، token و style حفظ می‌شوند.
4. خرید AI قیمت و مدت را Snapshot می‌کند.
5. درخواست تمدید، اشتراک فعال را خاموش نمی‌کند.
6. عملیات AI در سرویس مرکزی Transactional انجام می‌شود و توکن حتماً با botsaz تطبیق داده می‌شود.
7. رد درخواست تمدید، اشتراک فعال قبلی را حفظ می‌کند.
8. فعال‌سازی با تاریخ دقیق، وضعیت را واقعاً active می‌کند و تاریخ گذشته را نمی‌پذیرد.
9. نماینده اعلان نتیجه عملیات را در تلگرام دریافت می‌کند.
10. کلید API در HTML نمایش داده نمی‌شود و با Master Key رمزنگاری می‌شود.
11. وضعیت خاموش سراسری AI در ربات نماینده نیز رعایت می‌شود.
12. پاسخ AI پیش از ارسال در parse mode HTML ایمن می‌شود.
13. فایل‌های Compiled با Source و قالب‌های Default/Update یکسان‌سازی شدند.

## نتایج کنترل

- 20 مجموعه تست Static: موفق
- Source/Compiled parity: موفق
- Default/Update reseller bot parity: موفق
- Runtime eval: صفر
- TLS verification disabled: صفر
- نمایش Plaintext کلید AI در HTML: صفر
- منطق قدیمی که اشتراک فعال را هنگام تمدید pending می‌کرد: صفر

## محدودیت محیط بررسی

در محیط Workspace مفسر PHP و دیتابیس MySQL واقعی در دسترس نبود؛ بنابراین Parse و Runtime واقعی PHP/MySQL انجام نشد. تست نهایی Provider هوش مصنوعی، درگاه پرداخت و پنل VPN باید با Credential واقعی روی Staging یا هاست انجام شود.
