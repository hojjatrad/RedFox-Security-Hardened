# Red Fox 1.2 — عملیات سرویس، Migration Runner و رمزنگاری Secret

## ترتیب ارتقا

1. بکاپ کامل دیتابیس و فایل‌ها.
2. انتقال نسخه به Staging.
3. اجرای Migration Runner:

```bash
php bin/migrate.php --dry-run
php bin/migrate.php
```

Runner از قفل MySQL، checksum و جدول `schema_migrations` استفاده می‌کند. تغییر فایل migration اجراشده باعث توقف می‌شود.

## محصولات کاملاً اختصاصی نماینده

نماینده اکنون می‌تواند محصول اختصاصی با نام، قیمت، حجم، زمان و دسته موجود بسازد. رکورد با `agent = reseller_id` ذخیره می‌شود و Queryهای ربات نماینده طوری سخت‌سازی شده‌اند که فقط محصولات عمومی، نوع نمایندگی و شناسه مالک همان ربات را نمایش/خرید کنند. Callback دستکاری‌شده برای خرید محصول نماینده دیگر رد می‌شود.

## عملیات سرویس پنل نمایندگی

اگر دسترسی `extend_user` فعال باشد، نماینده می‌تواند از صفحه جزئیات سرویس:

- حجم اضافه کند؛
- زمان اضافه کند؛
- با یکی از محصولات مجاز تمدید کند.

هزینه از کیف پول **نماینده مالک سرویس** کم می‌شود، نه از مشتری. قیمت حجم/زمان از تنظیم پنل و قیمت تمدید از محصول و override ربات نماینده محاسبه می‌شود.

هر عملیات:

- مالکیت فاکتور را دوباره کنترل می‌کند؛
- کلید Idempotency یکتا دارد؛
- ابتدا مبلغ را رزرو می‌کند؛
- API پنل را فقط یک بار فراخوانی می‌کند؛
- در خطای قطعی، مبلغ را برمی‌گرداند؛
- اگر پنل موفق شده ولی ثبت محلی نامطمئن باشد، `needs_reconcile` می‌شود و خودکار تکرار نمی‌شود.

صفحه ادمین «عملیات نمایندگان» برای تطبیق دستی اضافه شده است. ادمین باید قبل از Mark Success یا Refund وضعیت واقعی پنل VPN را بررسی کند.

## محدودیت حفاظتی نام کانفیگ تکراری

کد Legacy برخی عملیات را با `username` پیدا می‌کند. اگر یک username در چند فاکتور وجود داشته باشد، پنل نمایندگی عملیات را متوقف می‌کند تا سرویس اشتباه تغییر نکند. این مورد باید ابتدا توسط ادمین رفع شود.

## رمزنگاری اطلاعات پنل VPN

برای فعال‌سازی، یک کلید ۳۲ بایتی بسازید:

```bash
openssl rand -hex 32
```

آن را با نام `REDFOX_MASTER_KEY` در Environment امن PHP-FPM/Apache و CLI قرار دهید. کلید را داخل Git یا web root نگذارید.

ابتدا Dry Run:

```bash
php bin/encrypt-secrets.php --dry-run
php bin/encrypt-secrets.php
```

فیلدهای زیر رمز می‌شوند:

- `password_panel`
- `api_key`
- `xui_api_token`
- `secret_code`

فرمت رمز دارای nonce تصادفی و احراز اصالت است (`secretbox` یا AES-256-GCM). اگر دیتابیس رمز شده باشد ولی کلید موجود نباشد، برنامه fail-closed می‌شود و ciphertext را به پنل مقصد ارسال نمی‌کند.

**گم‌شدن کلید یعنی credentialهای رمز‌شده قابل بازیابی نیستند؛ کلید را در Secret Manager و یک بکاپ آفلاین امن نگه دارید.**

## تست الزامی Staging

- extra volume/time روی هر نوع پنل مورد استفاده؛
- چهار روش مختلف Methodextend؛
- خطای عمدی API و بررسی Refund؛
- دوبار کلیک روی فرم و بررسی Idempotency؛
- قطع فرایند پس از پاسخ پنل و بررسی needs_reconcile؛
- اجرای Cron و تست اتصال پس از رمزنگاری Secret؛
- ویرایش credential پنل و اطمینان از ذخیره رمز‌شده.
