# رفع خطای طول Index در نصب — نسخه 2.2.5

خطای MySQL شماره 1071 به‌دلیل ستون قدیمی `Payment_report.id_order` با طول `VARCHAR(2000)` و ساخت Index کامل روی آن رخ می‌داد. در `utf8mb4` هر کاراکتر تا چهار بایت فضا می‌گیرد و طول کل Index از سقف 3072 بایت عبور می‌کرد.

اصلاحات:
- Index پرداخت روی `id_order(191)` و `payment_Status(50)` ساخته می‌شود.
- Index ربات‌ساز روی Prefix امن ستون‌های Token و User ساخته می‌شود.
- Index نماینده روی Prefixهای محدود ساخته می‌شود.
- Checksum نسخه‌های تاریخی که قبلاً با موفقیت اجرا شده‌اند همچنان پذیرفته می‌شود.
- اجرای مجدد نصب، داده‌ها را حذف نمی‌کند و Migration ناقص را ادامه می‌دهد.
