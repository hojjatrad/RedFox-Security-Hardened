# پایگاه دانش حرفه‌ای — نسخه 2.3.2

- اکشن گم‌شده `add_text` اضافه شد؛ متن کپی‌پیست واقعاً ذخیره می‌شود.
- ستون answer از TEXT به MEDIUMTEXT ارتقا یافت تا متن فارسی بزرگ با خطای Data too long مواجه نشود.
- متن تا ۲۵۰ هزار کاراکتر پذیرفته می‌شود.
- برای هر رکورد دکمه ویرایش و Modal حرفه‌ای اضافه شد.
- عنوان و متن کامل قابل ویرایش هستند و updated_at ثبت می‌شود.
- TXT، MD، CSV و PDF تا ۱۰ مگابایت پشتیبانی می‌شوند.
- نوع واقعی فایل و هدر PDF کنترل می‌شود؛ تغییر پسوند فایل مخرب پذیرفته نمی‌شود.
- استخراج PDF به‌ترتیب از Smalot (اگر موجود باشد)، pdftotext سرور و Parser داخلی امن استفاده می‌کند.
- PDF اسکن‌شده بدون متن با پیام واضح رد می‌شود و داده خراب ذخیره نمی‌شود.
- Sourceهای text و pdf در فهرست با برچسب فارسی نمایش داده می‌شوند.
