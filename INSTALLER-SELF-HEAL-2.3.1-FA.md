# نصب‌کننده خودترمیم و Release Gate — نسخه 2.3.1

## علت خطای نسخه قبلی
فایل Installer به `lib/ResellerBotSynchronizer.php` وابسته شده بود، اما فایل مذکور داخل ZIP نهایی قرار نگرفته بود. بسته‌ساز قدیمی فقط ZIP را ایجاد می‌کرد و وابستگی‌های Runtime را کنترل نمی‌کرد.

## اصلاح
- وابستگی خارجی مرحله همگام‌سازی حذف و کد آن داخل Installer قرار گرفت.
- همگام‌سازی ربات‌های نماینده اختیاری است؛ خطای آن دیگر نصب اصلی را متوقف نمی‌کند.
- قبل از هر تغییر دیتابیس، Installer فهرست کامل فایل‌های ضروری را کنترل می‌کند.
- اگر بسته ناقص باشد، نصب قبل از نوشتن config یا تغییر دیتابیس با فهرست دقیق فایل‌های مفقود متوقف می‌شود.
- Installer فایل‌های Compiled را از Source بازسازی می‌کند.
- اگر یکی از قالب‌های Default/Update ناقص باشد، آن را از قالب سالم مقابل بازسازی می‌کند.
- پوشه‌های storage، updates و Upload در صورت نبودن ساخته می‌شوند.

## Release Gate
اسکریپت `bin/build-release.py` تنها مسیر مجاز ساخت بسته است و موارد زیر را اجباری بررسی می‌کند:
- وجود و غیرخالی بودن تمام فایل‌های Runtime ضروری
- نبودن require به Helper حذف‌شده
- تطبیق کامل Source و Compiled
- تطبیق Default و Update ربات نماینده
- اجرای تمام تست‌های Static
- اجرای php -l در محیطی که PHP نصب است
- حضور تمام فایل‌های ضروری داخل ZIP نهایی
- سلامت ZIP و حداقل تعداد منطقی فایل‌ها
- حذف Secret، Log، Backup و Runtime data از Release
- تولید SHA-256

Release Gate در تست منفی نیز بررسی شد: با حذف موقت PanelCapacity ساخت ZIP متوقف شد و هیچ بسته ناقصی باقی نگذاشت.
