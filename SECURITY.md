# سیاست امنیت RedFox

## نسخه‌های پشتیبانی‌شده

| نسخه | وضعیت |
|---|---|
| `v2.4.12-security-hardened` | پشتیبانی‌شده |
| نسخه‌های قدیمی‌تر | فقط در صورت بازتولید روی آخرین release بررسی می‌شوند |

## گزارش خصوصی آسیب‌پذیری

آسیب‌پذیری را در Issue عمومی، Discussion، کانال تلگرام یا screenshot عمومی منتشر نکنید. در GitHub repository از مسیر **Security → Advisories → Report a vulnerability** گزارش خصوصی بسازید. اگر این گزینه برای حساب شما در دسترس نبود، ابتدا فقط یک پیام بدون جزئیات حساس برای مالک repository ارسال و کانال خصوصی هماهنگ کنید.

گزارش مفید شامل این موارد است:

- component و نسخه دقیق؛
- پیش‌نیازها و مراحل بازتولید؛
- اثر امنیتی؛
- proof of concept حداقلی و غیرمخرب؛
- پیشنهاد اصلاح، در صورت وجود؛
- اینکه vulnerability قبلاً عمومی شده است یا نه.

ارسال این موارد ممنوع است:

- token واقعی Telegram/GitHub/درگاه؛
- password، cookie یا session فعال؛
- `storage/secure.env.php`؛
- backup یا دیتابیس production؛
- اطلاعات شخصی کاربران.

## زمان پاسخ هدف

- تأیید دریافت: حداکثر ۷ روز؛
- triage اولیه: حداکثر ۱۴ روز؛
- زمان اصلاح بسته به شدت و پیچیدگی تعیین می‌شود.

این زمان‌ها هدف عملیاتی‌اند و قرارداد SLA نیستند.

## قواعد Safe Harbor

تحقیق باید روی دارایی خود پژوهشگر یا محیطی با اجازه صریح انجام شود. دسترسی به داده دیگران، اختلال سرویس، مهندسی اجتماعی، denial of service، spam، ماندگاری غیرمجاز و انتشار credential مجاز نیست. تا پیش از رفع و هماهنگی disclosure، جزئیات exploitable عمومی نشود.

## رخداد credential

اگر credential واقعی افشا شده است، منتظر patch نمانید: آن را فوراً revoke/rotate، sessionها را invalidate و logهای دسترسی را بررسی کنید. حذف credential از Git history به‌تنهایی آن را امن نمی‌کند.
