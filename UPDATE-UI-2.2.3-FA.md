# Red Fox 2.2.3 — بروزرسانی مستقیم و بکاپ اختیاری

- ورود به مرکز بروزرسانی دیگر کاربر را به صفحه Migration هدایت نمی‌کند.
- با فشردن «نصب نسخه جدید»، پنجره فارسی انتخاب بکاپ نمایش داده می‌شود.
- مدیر می‌تواند «بکاپ بگیر و ادامه بده» یا «ادامه بدون بکاپ دیتابیس» را انتخاب کند.
- بکاپ انتخابی در `storage/update-backups/JOB_ID/database/` ذخیره می‌شود.
- بکاپ فایل‌های تغییرکرده برای Rollback همیشه و مستقل از انتخاب دیتابیس ساخته می‌شود.
- خطای MySQL/PDO شماره 2014 با مصرف کامل نتیجه و `closeCursor()` در Backup و Migration رفع شد.
- جدول‌های داخلی مرکز بروزرسانی برای نصب‌های قدیمی در همان صفحه Bootstrap می‌شوند.
- بسته‌های جدید در Upload، updates و GitHub با پیام فارسی و دکمه نصب نمایش داده می‌شوند.
