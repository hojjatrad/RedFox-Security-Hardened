# نصب کامل Red Fox روی هاست اشتراکی

## پیش‌نیازها

- PHP 8.2 یا 8.3
- MySQL یا MariaDB
- گواهی SSL فعال
- افزونه‌های PDO MySQL، mysqli، cURL، ZIP، Sodium، JSON، mbstring و GD
- یک دیتابیس و کاربر دیتابیس؛ دیتابیس می‌تواند خالی یا دارای ساختار و اطلاعات نسخه قبلی Red Fox باشد
- دسترسی File Manager یا SSH برای خواندن توکن یک‌بارمصرف نصب

## مراحل نصب

1. فایل ZIP نصب کامل را داخل مسیر موردنظر هاست، مانند `public_html/bot`، بارگذاری کنید.
2. فایل را همان‌جا Extract کنید. فایل‌های `index.php`، `config.php` و پوشه `installer` باید مستقیماً در همان مسیر قرار بگیرند.
3. در مرورگر این آدرس را فقط با HTTPS باز کنید:

   `https://YOUR-DOMAIN/PATH/installer/`

4. در اولین درخواست، installer یک فایل مخفی با نام `.redfox-install-token` یا نام نام‌فضاشده‌ای مانند `.redfox-install-token-a1b2c3d4e5f6` می‌سازد. نام دقیق فایل روی صفحه ورود نشان داده می‌شود. در File Manager نمایش فایل‌های مخفی را فعال، فایل را در سطح بالاتر از ریشه وب پیدا و مقدار ۶۴ کاراکتری داخل آن را در صفحه ورود وارد کنید. توکن را در URL، پیام‌رسان یا اسکرین‌شات قرار ندهید.
5. آیدی عددی مدیر، توکن ربات و اطلاعات دیتابیس را وارد کنید. ربات باید قبلاً با حساب مدیر Start شده باشد.
6. نصب‌کننده به‌صورت خودکار:
   - اتصال امن به Telegram API و هویت ربات/مدیر را بررسی می‌کند؛
   - جداول پایه را می‌سازد؛
   - تمام Migrationهای نسخه جاری را اجرا می‌کند؛
   - کلیدهای رمزنگاری، بکاپ، Cron و Health را می‌سازد؛
   - حساب مدیر را با رمز تصادفی امن ایجاد می‌کند؛
   - Webhook دارای secret را ثبت می‌کند؛
   - دریافت پیام حاوی رمز موقت مدیر را کنترل می‌کند؛
   - فایل توکن و پوشه installer را پس از نصب موفق حذف می‌کند.
7. رمز موقت پنل مدیریت در پیام تلگرام برای مدیر ارسال می‌شود. پس از اولین ورود آن را تغییر دهید.

## رفع خطای ارتباط Telegram

installer به‌جای پیام مبهم، یک کد امن نشان می‌دهد:

- `TG-AUTH`: توکن توسط Telegram رد شده؛ از `@BotFather` توکن جدید بگیرید.
- `TG-DNS`: DNS هاست نتوانسته `api.telegram.org` را resolve کند.
- `TG-CONNECT` یا `TG-TIMEOUT`: خروجی TCP/443، فایروال، محدودیت کشور یا پراکسی هاست را بررسی کنید.
- `TG-TLS`: CA bundle و نسخه cURL/OpenSSL هاست را به‌روز کنید؛ TLS را غیرفعال نکنید.
- `TG-RESPONSE`: معمولاً WAF یا پراکسی به‌جای JSON تلگرام، HTML برگردانده است.

برای endpoint سازگار و مورداعتماد می‌توان در سطح environment سرور مقدار HTTPSِ `REDFOX_TELEGRAM_API_BASE` را تنظیم کرد. مقصد همچنان از policy عمومی DNS/IP و TLS اجباری عبور می‌کند؛ آدرس HTTP، داخلی یا self-signed پذیرفته نمی‌شود.

## مسیر پنل مدیریت

`https://YOUR-DOMAIN/PATH/panel/`

## نکات مهم

- نصب‌کننده دیتابیس موجود Red Fox را بدون حذف اطلاعات تکمیل می‌کند؛ با این حال اگر دیتابیس قبلاً استفاده شده است، پیش از نصب از آن خروجی SQL بگیرید.
- پوشه‌های `storage`، `Upload` و `updates` باید برای PHP قابل نوشتن باشند.
- توکن ربات و رمز دیتابیس بعد از خطا در HTML نگه‌داری نمی‌شوند و باید دوباره وارد شوند.
- پس از نصب، Cron Jobهای موردنیاز را از بخش «Wizard هاست» پنل مشاهده و در کنترل‌پنل هاست ثبت کنید.
