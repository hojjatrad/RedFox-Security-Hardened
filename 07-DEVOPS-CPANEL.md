# DEVOPS CPANEL

Validate PHP 8.4, required extensions, Composer, document root, environment configuration, permissions, HTTPS, database connectivity, Cron, webhooks, logs, storage, sessions, backups, cache and deployment/rollback. Ensure the app can run correctly on cPanel and VPS environments without hidden assumptions.
