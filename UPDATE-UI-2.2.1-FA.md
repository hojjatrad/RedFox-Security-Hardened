# Red Fox 2.2.1 — نصب ساده بروزرسانی

رابط نصب بسته‌ها ساده شد. دکمه Diff و دو ورودی «نسخه» و `UNSIGNED` از جدول بسته‌های آماده حذف شدند.

برای نصب اکنون فقط:

1. بسته معتبر را در فهرست انتخاب کنید؛
2. روی «⬆️ نصب بروزرسانی» بزنید؛
3. پیام تأیید فارسی را قبول کنید.

بررسی‌های امنیتی حذف نشده‌اند و پشت صحنه خودکار انجام می‌شوند:

- جدیدتر بودن نسخه؛
- Signed/Unsigned و Compatibility Mode؛
- ساختار ZIP و Path Traversal؛
- SHA-256 و Signature؛
- Diff فایل‌ها؛
- فهرست فایل‌های Preserve؛
- بکاپ دیتابیس و فایل؛
- Migration و Rollback.

اگر بسته بدون امضا باشد، پیام تأیید یک هشدار مشخص نمایش می‌دهد. پذیرش چنین بسته‌ای همچنان نیازمند فعال بودن گزینه «پذیرش Snapshot بدون امضا» در تنظیمات منبع است.
