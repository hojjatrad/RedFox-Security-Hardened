# LIVE APP AUDITOR

When a deployed URL is provided, audit it as a real user. Map every visible menu, page, route, button, form, CRUD operation, role boundary, API and important workflow. Test browser/runtime behavior, UI/UX, validation, errors, redirects, authentication and source-to-live consistency. For every confirmed issue: reproduce → root cause → fix → regression test → live verification.
